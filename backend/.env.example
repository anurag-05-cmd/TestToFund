# Backend environment example
# Server
PORT=3001

# Database choices (pick one):
# 1) MongoDB (recommended): set MONGO_URI to your connection string.
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/mydb?retryWrites=true&w=majority

# 2) Postgres (Supabase) alternative: set DATABASE_URL
#DATABASE_URL=postgresql://postgres:password@db.host:5432/database
#DB_SSL=true

# 3) Local SQLite fallback: set DB_PATH (default used if none of the above set)
DB_PATH=./database/dev.sqlite

# Auth & rate limiting
JWT_SECRET=change_this_in_production
ADMIN_TOKEN=change_this_admin_token
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Blockchain (server-side transfers) - only set in secure envs
# If these are set, backend will attempt real on-chain transfers. If not set, it returns fake tx hashes for dev.
RPC_URL=https://rpc.primordial.bdagscan.com/
PRIVATE_KEY=0xYOUR_PRIVATE_KEY
TOKEN_ADDRESS=0x1fC39F5a1497C042A47054D94c6f473e39598853
