{
    "name": "skill-to-earn-contracts",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "hardhat": "^2.17.0",
        "@nomicfoundation/hardhat-toolbox": "^2.0.0",
        "@openzeppelin/contracts": "^4.9.3",
        "dotenv": "^16.0.0"
    },
    "scripts": {
        "compile": "hardhat compile",
        "deploy:testnet": "hardhat run scripts/deploy.js --network testnet",
        "test": "hardhat test"
    }
}